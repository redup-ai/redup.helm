{{- if and $.Values.extraDeploy $.Values.extraDeploy.enable }}

{{- range $_, $data := $.Values.extraDeploy.data }}

---
{{- if kindIs "map" $data }}
{{ tpl (toYaml $data) $ | indent 0 }}
{{- else }}
{{ tpl ($data) $ | indent 0 }}
{{- end }}

{{- end }}

{{- end }}
